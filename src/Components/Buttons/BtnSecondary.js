
import '../../App.css'

const BtnPrimary = ({title}) => {
    return (
        <div>
            <button className='btn-secondary'>{title}</button>
        </div>
    );
}

export default BtnPrimary;